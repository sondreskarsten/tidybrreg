# Rename fields using field_dict; pass through unmapped fields All dict columns appear in output; absent fields become typed NA.

Rename fields using field_dict; pass through unmapped fields All dict
columns appear in output; absent fields become typed NA.

## Usage

``` r
rename_from_dict(flat)
```
