# Fetch påtegninger for a single entity from the enheter endpoint

Fetch påtegninger for a single entity from the enheter endpoint

## Usage

``` r
fetch_entity_paategninger(org_nr)
```

## Arguments

- org_nr:

  A 9-digit organization number.

## Value

A tibble of påtegning rows, or `NULL` if none.
