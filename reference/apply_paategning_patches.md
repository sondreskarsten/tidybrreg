# Apply påtegning-specific patches to the annotations state

Apply påtegning-specific patches to the annotations state

## Usage

``` r
apply_paategning_patches(paat_state, org_nr, patches, timestamp, update_id)
```
