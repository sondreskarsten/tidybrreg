# Flatten nested rollegrupper/roller JSON to a tibble

Flatten nested rollegrupper/roller JSON to a tibble

## Usage

``` r
flatten_roles(raw, org_nr)
```

## Arguments

- raw:

  Parsed JSON list from the roles endpoint.

- org_nr:

  Organization number (passed through to output).
