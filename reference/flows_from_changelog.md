# Compute flows from the persistent changelog

Compute flows from the persistent changelog

## Usage

``` r
flows_from_changelog(
  by = c("nace_1", "municipality_code"),
  from = NULL,
  to = NULL
)
```
