# Build an httr2 request to the SSB KLASS API

Build an httr2 request to the SSB KLASS API

## Usage

``` r
klass_req(path, query = list())
```

## Arguments

- path:

  URL path appended to the base URL.

- query:

  Named list of query parameters.
