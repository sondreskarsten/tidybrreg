# Build a manifest entry from download metadata

Build a manifest entry from download metadata

## Usage

``` r
build_manifest_entry(
  type,
  snapshot_date,
  endpoint,
  format,
  resp = NULL,
  raw_path = NULL,
  parquet_path = NULL,
  record_count = NULL
)
```
