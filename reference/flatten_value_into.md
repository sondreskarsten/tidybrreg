# Recursively flatten a patch value, calling emit() for each leaf

Recursively flatten a patch value, calling emit() for each leaf

## Usage

``` r
flatten_value_into(op, path_prefix, value, emit)
```
