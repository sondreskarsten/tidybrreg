% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/roller-diff.R
\name{diff_roller_state}
\alias{diff_roller_state}
\title{Compute field-level diffs between two roller state tibbles}
\usage{
diff_roller_state(
  old_state,
  new_state,
  timestamp = format(Sys.time(), "\%Y-\%m-\%dT\%H:\%M:\%S"),
  update_id = NA_integer_
)
}
\arguments{
\item{old_state}{Tibble. Previous roller state from
\code{\link[=read_state]{read_state()}} or an earlier \code{\link[=flatten_roles]{flatten_roles()}} call.
\code{NULL} or zero-row tibble treats all current roles as additions.}

\item{new_state}{Tibble. Current roller state from
\code{\link[=brreg_download]{brreg_download()}} or \code{\link[=flatten_roles]{flatten_roles()}}.}

\item{timestamp}{Character or POSIXct. Timestamp for changelog
entries (typically the CDC event time or sync time).}

\item{update_id}{Integer or character. Identifier for the sync
batch, used as \code{update_id} in the changelog.}
}
\value{
A tibble matching the changelog schema: \code{timestamp},
\code{org_nr}, \code{registry} (always \code{"roller"}), \code{change_type}
(\code{"entry"}, \code{"exit"}, \code{"change"}), \code{field}, \code{value_from},
\code{value_to}, \code{update_id}.
}
\description{
Pure function: takes two flattened roller tibbles (as produced by
\code{\link[=flatten_roles]{flatten_roles()}} or \code{\link[=parse_roles_bulk]{parse_roles_bulk()}}) and returns a long-format
changelog recording every field-level mutation. Detects three
categories of change: role additions, role removals, and
field-level modifications on continuing roles.
}
\section{Composite key}{

Each role assignment is identified by
\verb{(org_nr, role_group_code, role_code, holder_id)} where \code{holder_id}
is derived from \code{person_id} for person-held roles and
\code{entity:{entity_org_nr}} for entity-held roles (auditors,
accountants). Roles with neither are keyed as \code{unknown:{row_index}}
within their respective state — these are rare and produce
conservative add/remove pairs rather than false modifications.
}

\section{NA handling}{

For additions, fields that are \code{NA} in the new state are excluded
from the changelog (no value to report). For removals, fields that
are \code{NA} in the old state are excluded. For modifications, a change
from \code{NA} to a non-NA value (or vice versa) is recorded.
}

\seealso{
Other tidybrreg data management functions: 
\code{\link{brreg_annotation_summary}()},
\code{\link{brreg_annotations}()},
\code{\link{brreg_status}()},
\code{\link{brreg_sync}()},
\code{\link{brreg_sync_status}()},
\code{\link{read_changelog}()}
}
\concept{tidybrreg data management functions}
