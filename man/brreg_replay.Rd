% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/replay.R
\name{brreg_replay}
\alias{brreg_replay}
\title{Reconstruct register state by replaying CDC updates}
\usage{
brreg_replay(base, updates, target_date = Sys.Date(), cols = NULL)
}
\arguments{
\item{base}{A tibble from \code{\link[=brreg_download]{brreg_download()}}, \code{\link[=brreg_import]{brreg_import()}}, or
a snapshot read via \code{\link[=brreg_open]{brreg_open()}}. Must contain \code{org_nr}.}

\item{updates}{A tibble from \code{\link[=brreg_updates]{brreg_updates()}} with
\code{include_changes = TRUE}. Must contain \code{org_nr}, \code{change_type},
\code{timestamp}, and \code{changes} (list-column of patch tibbles).}

\item{target_date}{Date. Reconstruct state as of this date.
Only updates with \code{timestamp <= target_date} are applied.}

\item{cols}{Character vector of columns to track. If \code{NULL},
all columns present in \code{base} are used.}
}
\value{
A tibble with the same columns as \code{base}, reflecting
all applied changes up to \code{target_date}. Attribute
\code{replay_info} records the number of inserts, updates, and
deletes applied.
}
\description{
Given a base snapshot and a stream of CDC updates from
\code{\link[=brreg_updates]{brreg_updates()}}, reconstruct the state of the register at any
arbitrary date. Uses \code{dplyr::rows_upsert()} for Ny/Endring events
and \code{dplyr::rows_delete()} for Sletting events, applied
chronologically.
}
\section{Limitations}{

The brreg CDC stream provides only new values (not old values)
in RFC 6902 JSON Patch format, and field-level changes are only
available from September 2025. Before that date, only the
change type (Ny/Endring/Sletting) is recorded — field-level
replay is not possible for those periods. Use \code{\link[=brreg_events]{brreg_events()}}
(snapshot diff) for pre-September 2025 field-level changes.
}

\examples{
\dontshow{if (FALSE) withAutoprint(\{ # examplesIf}
base <- brreg_download(type_output = "tibble")
updates <- brreg_updates(since = Sys.Date() - 30,
                          size = 10000, include_changes = TRUE)
state <- brreg_replay(base, updates, target_date = Sys.Date())
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=brreg_panel]{brreg_panel()}} for multi-snapshot panels,
\code{\link[=brreg_updates]{brreg_updates()}} to fetch the CDC stream.

Other tidybrreg panel functions: 
\code{\link{as_brreg_tsibble}()},
\code{\link{brreg_events}()},
\code{\link{brreg_panel}()},
\code{\link{brreg_series}()}
}
\concept{tidybrreg panel functions}
