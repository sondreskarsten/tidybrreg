% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/roles.R
\name{brreg_roles}
\alias{brreg_roles}
\title{Retrieve board members, officers, and auditors}
\usage{
brreg_roles(org_nr)
}
\arguments{
\item{org_nr}{Character. 9-digit organization number.}
}
\value{
A tibble with one row per role assignment. Columns:
\code{org_nr}, \code{role_group}, \code{role_group_code}, \code{role}, \code{role_code},
\code{first_name}, \code{middle_name}, \code{last_name}, \code{birth_date}, \code{deceased},
\code{entity_org_nr}, \code{entity_name}, \code{resigned}, \code{person_id}.
Returns an empty tibble if the entity has no registered roles.
}
\description{
Fetch all registered roles for a Norwegian legal entity. Returns one
row per role assignment. Person-held roles include name and birth date.
Entity-held roles (auditor firms, accountants) include the entity's
organization number.
}
\details{
Role types and groups are returned as English labels looked up from
the package's \link{role_types} and \link{role_groups} reference datasets.
Original Norwegian codes are preserved in \code{role_code} and
\code{role_group_code}.
}
\section{Person identification}{

The \code{person_id} column is a synthetic key composed of birth date, last
name, first name, and middle name. It enables network analysis across
companies but has a non-trivial collision risk for common Norwegian
names sharing a birth date. The brreg public API does not expose
national identity numbers.
}

\examples{
\dontshow{if (interactive() && curl::has_internet()) withAutoprint(\{ # examplesIf}
brreg_roles("923609016")  # Equinor ASA
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=brreg_board_summary]{brreg_board_summary()}} for derived board covariates,
\link{role_types} and \link{role_groups} for the English lookup tables.

Other tidybrreg entity functions: 
\code{\link{brreg_board_summary}()},
\code{\link{brreg_download}()},
\code{\link{brreg_entity}()},
\code{\link{brreg_roles_legal}()},
\code{\link{brreg_search}()},
\code{\link{brreg_updates}()}
}
\concept{tidybrreg entity functions}
