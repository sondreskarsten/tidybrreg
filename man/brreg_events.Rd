% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/events.R
\name{brreg_events}
\alias{brreg_events}
\title{Detect changes between two snapshots}
\usage{
brreg_events(
  date_from,
  date_to,
  cols = NULL,
  type = c("enheter", "underenheter")
)
}
\arguments{
\item{date_from, date_to}{Dates identifying the two snapshots to
compare. Both must exist in the snapshot store. \code{date_from} is
the "before" state, \code{date_to} is the "after" state.}

\item{cols}{Character vector of columns to track for changes.
\code{NULL} (default) tracks all columns in \link{field_dict}.}

\item{type}{One of \code{"enheter"} or \code{"underenheter"}.}
}
\value{
A tibble with columns: \code{org_nr}, \code{event_type}
(\code{"entry"}, \code{"exit"}, \code{"change"}), \code{event_date} (the \code{date_to}
snapshot date), \code{field} (column name, \code{NA} for entry/exit),
\code{value_from} (character, previous value), \code{value_to} (character,
new value).
}
\description{
Compare two dated snapshots and return a tibble of entity-level
events: entries (new entities), exits (deleted entities), and
field-level changes. Unlike the CDC stream, snapshot diffs provide
both old and new values for every changed field.
}
\examples{
\dontshow{if (interactive() && requireNamespace("arrow", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\donttest{
snaps <- brreg_snapshots()
if (nrow(snaps) >= 2) {
  events <- brreg_events(snaps$snapshot_date[1], snaps$snapshot_date[2])
  events
}
}
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=brreg_updates]{brreg_updates()}} for the CDC stream (API-level changes),
\code{\link[=brreg_panel]{brreg_panel()}} for full panel construction.

Other tidybrreg panel functions: 
\code{\link{as_brreg_tsibble}()},
\code{\link{brreg_panel}()},
\code{\link{brreg_replay}()},
\code{\link{brreg_series}()}
}
\concept{tidybrreg panel functions}
