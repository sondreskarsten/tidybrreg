% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/panel.R
\name{brreg_panel}
\alias{brreg_panel}
\title{Construct a firm-period panel from accumulated snapshots}
\usage{
brreg_panel(
  frequency = c("year", "quarter", "month", "custom"),
  cols = NULL,
  from = NULL,
  to = NULL,
  dates = NULL,
  type = c("enheter", "underenheter", "roller"),
  label = FALSE
)
}
\arguments{
\item{frequency}{One of \code{"year"} (default), \code{"quarter"}, \code{"month"},
or \code{"custom"}. For \code{"custom"}, supply target dates via \code{dates}.}

\item{cols}{Character vector of column names to include. \code{NULL}
(default) returns all columns mapped by \link{field_dict}.}

\item{from, to}{Start and end dates for the panel. \code{NULL} defaults
to the range of available snapshots.}

\item{dates}{Date vector for \code{frequency = "custom"}.}

\item{type}{One of \code{"enheter"} or \code{"underenheter"}.}

\item{label}{Logical. If \code{TRUE}, apply \code{\link[=brreg_label]{brreg_label()}} to the
result.}
}
\value{
A tibble with columns: \code{org_nr}, \code{period} (character label
for the period), \code{snapshot_date} (the actual snapshot used),
plus requested \code{cols}. Attribute \code{date_mapping} records which
snapshot was used for each period.
}
\description{
Build an unbalanced firm × period panel from the Hive-partitioned
snapshot store. For each target period, selects the most recent
prior snapshot (LOCF). Requires at least two snapshots saved via
\code{\link[=brreg_snapshot]{brreg_snapshot()}} or \code{\link[=brreg_import]{brreg_import()}}.
}
\examples{
\dontshow{if (interactive() && requireNamespace("arrow", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\donttest{
# Annual panel of all entities
panel <- brreg_panel()

# Monthly panel for specific columns
panel <- brreg_panel("month", cols = c("employees", "nace_1", "legal_form"))
}
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=brreg_snapshot]{brreg_snapshot()}} to accumulate snapshots,
\code{\link[=brreg_events]{brreg_events()}} for change detection between snapshots.

Other tidybrreg panel functions: 
\code{\link{as_brreg_tsibble}()},
\code{\link{brreg_events}()},
\code{\link{brreg_replay}()},
\code{\link{brreg_series}()}
}
\concept{tidybrreg panel functions}
