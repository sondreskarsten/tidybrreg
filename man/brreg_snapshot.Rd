% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/snapshot.R
\name{brreg_snapshot}
\alias{brreg_snapshot}
\title{Save a dated snapshot of the full register}
\usage{
brreg_snapshot(
  type = c("enheter", "underenheter"),
  date = Sys.Date(),
  force = FALSE,
  ask = interactive()
)
}
\arguments{
\item{type}{One of \code{"enheter"} (main entities, default) or
\code{"underenheter"} (sub-entities / establishments).}

\item{date}{Date for this snapshot (default: today). Used as the
partition key, not as an API parameter — the brreg bulk endpoint
always returns the current-day state.}

\item{force}{Logical. If \code{TRUE}, overwrite an existing partition for
this date. Default \code{FALSE} skips if partition exists.}

\item{ask}{Logical. If \code{TRUE} (the default in interactive sessions),
prompt before downloading ~145 MB.}
}
\value{
The file path to the written Parquet partition (invisibly).
}
\description{
Download today's complete register and save as a Parquet partition
in the local snapshot store. Each call adds one partition to a
Hive-partitioned dataset at
\verb{tools::R_user_dir("tidybrreg", "data")/\{type\}/snapshot_date=\{date\}/}.
Subsequent calls to \code{\link[=brreg_panel]{brreg_panel()}} and \code{\link[=brreg_events]{brreg_events()}} query this
partitioned dataset lazily via \code{arrow::open_dataset()}.
}
\examples{
\dontshow{if (interactive() && curl::has_internet() && tidybrreg:::parquet_tier() != "none") withAutoprint(\{ # examplesIf}
\donttest{
brreg_snapshot()
brreg_snapshots()
}
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=brreg_import]{brreg_import()}} to add historical snapshots from CSV files,
\code{\link[=brreg_snapshots]{brreg_snapshots()}} to list available snapshots,
\code{\link[=brreg_panel]{brreg_panel()}} to construct panels from accumulated snapshots.

Other tidybrreg snapshot functions: 
\code{\link{brreg_cleanup}()},
\code{\link{brreg_data_dir}()},
\code{\link{brreg_import}()},
\code{\link{brreg_open}()},
\code{\link{brreg_snapshots}()}
}
\concept{tidybrreg snapshot functions}
