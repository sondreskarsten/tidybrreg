% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/signatur.R
\name{brreg_signatur}
\alias{brreg_signatur}
\title{Retrieve signature authority (signaturrett)}
\usage{
brreg_signatur(org_nr)
}
\arguments{
\item{org_nr}{Character. 9-digit organization number.}
}
\value{
A tibble with one row per person per signing combination.
Columns: \code{org_nr}, \code{entity_name}, \code{signature_type}, \code{rule_status},
\code{rule_text}, \code{combination_id}, \code{combination_code}, \code{rule}, \code{name},
\code{birth_date}, \code{role_code}, \code{role}. Returns an empty tibble if the
entity has no registered signing combination.
}
\description{
Fetch the registered signing combinations for a Norwegian legal
entity from the Brønnøysund Fullmakt service. Signature authority
determines who may bind the entity in general, and is distinct from
the roles returned by \code{\link[=brreg_roles]{brreg_roles()}}: an entity may have many board
members yet a signing rule such as "the board jointly" or "the
general manager alone".
}
\details{
Each registered combination (\code{combination_id}) carries a rule
(\code{rule}, e.g. "Styret i fellesskap") and the persons who satisfy it.
One row is returned per person within each combination.
}
\section{Person identification}{

The Fullmakt service returns each person's name as a single string in
\code{name}; it does not split it into given and family names, so no
synthetic \code{person_id} is constructed. Join to \code{\link[=brreg_roles]{brreg_roles()}} on
\code{birth_date} together with \code{name} at query time if cross-referencing
to the role network is required. \code{role_code} carries the registry's
role code (joinable to \link{role_types}); \code{role} is the registry's own
Norwegian description.
}

\section{Standardised vs registered rule}{

\code{rule} and \code{combination_code} come from the registry's structured
combination. Where the rule has been standardised (\code{rule_status} code
\code{"RF"}) this is the registered rule. Where it has not (\code{"RI"}), the
structured combination is the statutory default ("the board jointly")
and the actually registered rule is the free text in \code{rule_text};
read \code{rule_text} in that case. \code{rule_text} may be \code{NA} when no free
text is recorded.
}

\examples{
\dontshow{if (interactive() && curl::has_internet()) withAutoprint(\{ # examplesIf}
brreg_signatur("923609016") # Equinor ASA
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=brreg_prokura]{brreg_prokura()}} for procuration, \code{\link[=brreg_roles]{brreg_roles()}} for the
underlying role assignments.

Other tidybrreg entity functions:
\code{\link[=brreg_board_summary]{brreg_board_summary()}},
\code{\link[=brreg_children]{brreg_children()}},
\code{\link[=brreg_download]{brreg_download()}},
\code{\link[=brreg_entity]{brreg_entity()}},
\code{\link[=brreg_prokura]{brreg_prokura()}},
\code{\link[=brreg_roles]{brreg_roles()}},
\code{\link[=brreg_roles_legal]{brreg_roles_legal()}},
\code{\link[=brreg_search]{brreg_search()}},
\code{\link[=brreg_underenheter]{brreg_underenheter()}},
\code{\link[=brreg_update_fields]{brreg_update_fields()}},
\code{\link[=brreg_updates]{brreg_updates()}}
}
\concept{tidybrreg entity functions}
