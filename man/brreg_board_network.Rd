% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/governance.R
\name{brreg_board_network}
\alias{brreg_board_network}
\title{Build a director interlock network}
\usage{
brreg_board_network(org_nrs = NULL, roles_data = NULL)
}
\arguments{
\item{org_nrs}{Character vector of organization numbers to include.
Roles are fetched via \code{\link[=brreg_roles]{brreg_roles()}} for each entity.}

\item{roles_data}{Optional pre-fetched roles tibble (from
\code{\link[=brreg_roles]{brreg_roles()}}). If provided, \code{org_nrs} is ignored.}
}
\value{
A \code{tbl_graph} with two node types: \code{"entity"} (identified
by \code{org_nr}) and \code{"person"} (identified by \code{person_id}). Edge
attributes include \code{role_code}, \code{role_group_code}, and \code{org_nr}.
}
\description{
Construct a bipartite graph of entities and persons linked by
board/officer roles. Returns a \code{tbl_graph} (tidygraph) object
suitable for centrality analysis and ggraph visualization.
}
\examples{
\dontshow{if (interactive() && curl::has_internet() && requireNamespace("tidygraph", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\donttest{
net <- brreg_board_network(c("923609016", "984851006"))
net
}
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=brreg_roles]{brreg_roles()}} to fetch role data,
\code{\link[=brreg_board_summary]{brreg_board_summary()}} for board-level covariates.

Other tidybrreg governance functions: 
\code{\link{brreg_survival_data}()}
}
\concept{tidybrreg governance functions}
