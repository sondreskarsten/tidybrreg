% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network.R
\name{brreg_network}
\alias{brreg_network}
\title{Build an entity network graph}
\usage{
brreg_network(
  org_nr,
  depth = 1L,
  include = c("underenheter", "children", "roles", "legal_roles"),
  download = FALSE
)
}
\arguments{
\item{org_nr}{Character vector of seed organization numbers.}

\item{depth}{Integer. 0 = seed only, 1 = ego network (default),
2 = expand through persons (requires bulk data).}

\item{include}{Character vector of relationship types to include.
Default includes all available types. Current types:
\code{"underenheter"}, \code{"children"}, \code{"roles"}, \code{"legal_roles"}.}

\item{download}{Logical. If \code{TRUE} and depth > 1, offer to
download missing bulk data interactively. Default \code{FALSE}.}
}
\value{
A \code{tbl_graph} with node attributes \code{node_id}, \code{node_type},
\code{name}, \code{org_nr}, \code{person_id}, and edge attributes \code{from}, \code{to},
\code{edge_type}, \code{role_code}, \code{role}.
}
\description{
Construct a \code{tbl_graph} (tidygraph) representing the relationships
around one or more seed entities. At depth 1, the graph includes
sub-units, child entities, role holders, and legal role targets —
all reachable via direct API calls. At depth 2, the graph expands
through person nodes to discover board interlocks, requiring local
bulk data (see Details).
}
\section{Depth and data requirements}{

\itemize{
\item \strong{Depth 0}: Seed entity only. 1 API call per seed.
\item \strong{Depth 1}: Full ego network. 5-7 API calls per seed.
\item \strong{Depth 2}: Board interlocks via person-to-entity reverse lookup.
Requires local bulk data for enheter, underenheter, and roller.
Run \code{\link[=brreg_snapshot]{brreg_snapshot()}} for each type first, or call with
\code{download = TRUE} to trigger downloads interactively.
}
}

\section{Extensibility}{

The \code{include} parameter controls which relationship types are
traversed. Each type maps to an internal collector function.
Future versions may add types such as \code{"addresses"},
\code{"prior_owners"}, or \code{"accounting"}.
}

\examples{
\dontshow{if (interactive() && curl::has_internet() && requireNamespace("tidygraph", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\donttest{
net <- brreg_network("923609016")
net

tidygraph::as_tibble(net, "nodes")
tidygraph::as_tibble(net, "edges")
}
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=brreg_entity]{brreg_entity()}} for single lookups,
\code{\link[=brreg_board_network]{brreg_board_network()}} for the roles-only subgraph,
\code{\link[=brreg_status]{brreg_status()}} to check bulk data availability.

Other tidybrreg governance functions: 
\code{\link{brreg_board_network}()},
\code{\link{brreg_survival_data}()}
}
\concept{tidybrreg governance functions}
