% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/governance.R
\name{brreg_survival_data}
\alias{brreg_survival_data}
\title{Prepare firm survival data}
\usage{
brreg_survival_data(
  data,
  entry_var = "founding_date",
  censoring_date = Sys.Date()
)
}
\arguments{
\item{data}{A tibble from \code{\link[=brreg_download]{brreg_download()}}, \code{\link[=brreg_search]{brreg_search()}},
or \code{\link[=brreg_panel]{brreg_panel()}}. Must contain at least \code{org_nr} and a
date column for entry.}

\item{entry_var}{Column name for the entry date. Default
\code{"founding_date"} (stiftelsesdato).}

\item{censoring_date}{Date at which surviving firms are
right-censored. Default: today.}
}
\value{
A tibble with added columns: \code{entry_date}, \code{exit_date}
(Date or NA), \code{duration_years} (numeric), \code{event} (integer:
1 = exit observed, 0 = right-censored). Compatible with
\code{survival::Surv(duration_years, event)}.
}
\description{
Compute time-to-event and censoring indicators from entity
registration data, ready for use with \code{survival::Surv()} and
\code{flexsurv}.
}
\examples{
\dontshow{if (interactive() && curl::has_internet()) withAutoprint(\{ # examplesIf}
\donttest{
firms <- brreg_search(legal_form = "AS", municipality_code = "0301",
                       max_results = 100)
surv <- brreg_survival_data(firms)
surv[, c("org_nr", "entry_date", "exit_date", "duration_years", "event")]
}
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=brreg_download]{brreg_download()}} for full register data.

Other tidybrreg governance functions: 
\code{\link{brreg_board_network}()},
\code{\link{brreg_network}()}
}
\concept{tidybrreg governance functions}
