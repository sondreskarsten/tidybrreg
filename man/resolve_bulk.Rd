% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bulk-check.R
\name{resolve_bulk}
\alias{resolve_bulk}
\title{Resolve a single bulk dataset with session caching}
\usage{
resolve_bulk(type)
}
\arguments{
\item{type}{One of \code{"enheter"}, \code{"underenheter"}, \code{"roller"}.}
}
\value{
An Arrow Table, tibble, or NULL if not available.
}
\description{
Resolution order:
\enumerate{
\item Session cache in \code{.brregEnv} (keyed by type + snapshot date)
\item Parquet snapshot — Arrow Table if arrow installed, else tibble
\item Download cache — raw JSON/CSV parsed to tibble
}
}
\details{
Arrow Tables are zero-copy memory maps (~0 bytes until filtered).
Tibbles from nanoparquet or raw cache load the full dataset eagerly
(~2 GB for roller, ~1.5 GB for enheter).
}
\keyword{internal}
