% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tsibble.R
\name{as_brreg_tsibble}
\alias{as_brreg_tsibble}
\title{Convert tidybrreg output to tsibble}
\usage{
as_brreg_tsibble(x, key = NULL, index = NULL)
}
\arguments{
\item{x}{A tibble from \code{\link[=brreg_panel]{brreg_panel()}} or \code{\link[=brreg_series]{brreg_series()}}.}

\item{key}{Character vector of key column(s). For panels, typically
\code{"org_nr"}. For series, the grouping variable (e.g. \code{"legal_form"}).
If \code{NULL}, inferred from the \code{brreg_panel_meta} attribute.}

\item{index}{Character. Name of the time index column. Default
\code{"period"} for series output, \code{"snapshot_date"} for panel output.}
}
\value{
A tsibble.
}
\description{
Convert the output of \code{\link[=brreg_panel]{brreg_panel()}} or \code{\link[=brreg_series]{brreg_series()}} to a
tsibble for use with the tidyverts ecosystem (fable, feasts,
slider). Uses \code{regular = FALSE} since brreg snapshots are
irregularly spaced.
}
\examples{
\dontshow{if (interactive() && requireNamespace("tsibble", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\donttest{
panel <- brreg_panel(cols = c("employees", "legal_form"))
as_brreg_tsibble(panel)
}
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=brreg_panel]{brreg_panel()}}, \code{\link[=brreg_series]{brreg_series()}}.

Other tidybrreg panel functions: 
\code{\link{brreg_events}()},
\code{\link{brreg_panel}()},
\code{\link{brreg_replay}()},
\code{\link{brreg_series}()}
}
\concept{tidybrreg panel functions}
